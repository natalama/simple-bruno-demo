meta {
  name: Get the exchange rate for today
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/{{currentDate}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.amount: eq 1
  parseInt(res("..PHP")): gte 50.00
}
